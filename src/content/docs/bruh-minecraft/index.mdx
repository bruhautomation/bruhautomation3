---
title: BRUH Minecraft Server
description: A Minecraft server that lives inside Home Assistant. Bedrock cross-play, git-versioned worlds, RCON, plugins, and a full panel — zero VPS required.
template: splash
hero:
  tagline: A production Minecraft server inside Home Assistant — with Bedrock cross-play, world version control, mobile-friendly panel, and a first-class HA integration.
  image:
    file: ../../../assets/bruh-logo-dark.svg
  actions:
    - text: Quick Start
      link: /bruh-minecraft/quickstart/
      icon: right-arrow
    - text: Configuration Reference
      link: /bruh-minecraft/reference/
      icon: open-book
      variant: minimal
    - text: GitHub
      link: https://github.com/bruhautomation/BRUH-HA-Apps/tree/main/bruh-minecraft-server
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## What it is

A Home Assistant add-on that runs a real Minecraft Java Edition server — Paper, Purpur, Folia, Vanilla, Fabric, or Forge — with a management panel in your HA sidebar. No VPS. No proxy jars. No Microsoft account required when you don't want one.

![BRUH Minecraft management panel](/images/bruh-minecraft/panel-dashboard.png)

## For the players

<CardGrid>
  <Card title="Bedrock cross-play" icon="phone">
    iOS, Android, Switch, Xbox, PS, Win10/11. Auto-installs Geyser + Floodgate. **LAN auto-discovery** — the server appears in the **Friends** tab on any device on the same subnet, no manual IP entry.
  </Card>
  <Card title="No-Xbox family mode" icon="approve-check">
    Flip `online_mode: false` and the add-on silently fixes every downstream setting — secure-profile, Geyser auth, Floodgate, Bedrock validation. Kids without Microsoft accounts join in seconds.
  </Card>
  <Card title="One-click cheats + ops" icon="rocket">
    `allow_cheats: true` enables `/gamemode`, `/give`, `/tp`, `/summon`, `/fill`. List your username under `initial_ops` and you're OP'd automatically on first boot — works in offline mode too.
  </Card>
  <Card title="Voice Chat ready" icon="comment-alt">
    Port 24454/UDP is exposed by default. Drop the Simple Voice Chat plugin into the `plugins:` list and players get spatial in-game voice.
  </Card>
</CardGrid>

## For the operator

<CardGrid>
  <Card title="Any flavour, any version" icon="seti:default">
    Paper, Purpur, Folia, Vanilla, Fabric, Forge. `LATEST`, `SNAPSHOT`, or pinned. Aikar G1GC tuning enabled by default. Jars are cached so restarts work even when offline.
  </Card>
  <Card title="Git-versioned worlds" icon="document">
    Snapshots on a schedule, one-click restore from the panel, full commit history. Tar.gz archive mode for very large worlds.
  </Card>
  <Card title="Multi-world profiles" icon="random">
    Run a creative sandbox *and* a hard-survival world side by side, each with its own world files, plugins, and ops list. Switch with one click.
  </Card>
  <Card title="Declarative plugins" icon="list-format">
    Drop URLs in the `plugins:` list and they install on every boot — with `If-Modified-Since`, so unchanged jars don't re-download. Bad URLs warn and skip; can't tank startup.
  </Card>
  <Card title="Self-healing reliability" icon="approve-check">
    Crash auto-restart (rate-limited 5/5min). Auto-kicker for ghost sessions on iOS retries. Connection-throttle controls. Optional idle-timeout kick. Daily scheduled restart for memory creep.
  </Card>
  <Card title="Mobile-friendly panel" icon="puzzle">
    Fully responsive — horizontal-scroll tab row, 44px touch targets, 16px input fonts (no iOS auto-zoom). The HA Companion app experience matches desktop.
  </Card>
  <Card title="Works offline" icon="seti:lock">
    Once a jar is cached, the server starts even when your HA host has no internet. Plugin and Geyser auto-updates skip; cached jars are reused. LAN-only setups Just Work.
  </Card>
</CardGrid>

## The panel

A tab for everything you'd want to do without SSHing in:

![Panel tabs](/images/bruh-minecraft/panel-tabs.png)

- **Dashboard** — status, uptime, memory, TPS, online players, quick `/say`, one-shot RCON
- **Console** — live colour-coded JVM log + command bar
- **Players** — op / kick / ban / whitelist with one click
- **Server Properties** — live editor with RCON apply
- **Plugins** — install by URL, list with size/mtime, delete
- **Backups** — git snapshots and archives, one-click restore
- **Worlds** — switch between profiles

Always-visible header buttons: **Backup** • **Update** • **Restart** (JVM-only, ~15s) • **Stop**.

## Home Assistant integration

A first-class companion integration, auto-discovered on first boot:

| What | Includes |
|------|----------|
| **12 sensors** | `players_online` (with names list), TPS 1m/5m/15m, latency, uptime, version, MOTD, difficulty, gamemode, server_type |
| **2 binary sensors** | `reachable`, `rcon_ok` |
| **4 buttons** | `backup_now`, `restart_server`, `stop_server`, `save_all` |
| **13 services** | `say`, `rcon_command`, `give`, `set_weather`, `set_time`, lifecycle (`backup_now` / `restart_server` / `stop_server`), player ops (`op_player` / `deop_player` / `kick_player` / `ban_player` / `whitelist_add` / `whitelist_remove`) |
| **Notify platform** | `notify.bruh_minecraft_broadcast` — broadcast to in-game chat from any HA notification flow |

Pair with the [BRUH Claude](/bruh-claude/) add-on and you can say *"tell everyone dinner is ready"* and Claude will hit the `say` service for you.

## A taste

```yaml
# Auto-stop when nobody's playing for 30 minutes
automation:
  - alias: Minecraft - auto-stop when idle
    trigger:
      platform: numeric_state
      entity_id: sensor.bruh_minecraft_players_online
      below: 1
      for: "00:30:00"
    action:
      service: bruh_minecraft.stop_server

# Snapshot before any risky world edit
script:
  safe_fill:
    sequence:
      - service: bruh_minecraft.backup_now
      - service: bruh_minecraft.rcon_command
        data: { command: "fill ~-10 ~ ~-10 ~10 ~ ~10 minecraft:stone" }

# Broadcast a doorbell ring to everyone in-game
service: notify.bruh_minecraft_broadcast
data: { message: "Doorbell rang!" }
```

## Up and running

1. Add the **BRUH HA Apps** repository in HA.
2. Install **BRUH Minecraft Server**.
3. Set `eula: true` in the **Configuration** tab.
4. Click **Start** and connect on Java `:25565` or Bedrock `:19132`.

[Full Quick Start →](/bruh-minecraft/quickstart/)
