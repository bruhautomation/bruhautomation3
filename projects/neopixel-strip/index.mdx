---
title: Neopixel Strip
description: Addressable RGB LED strip with effects and OTA updates.
# Not on the live site until the write-up is finished:
# `draft` keeps it on dev and Vercel preview only. Remove the line to publish.
draft: true
---

import ProjectFiles from '../../site/components/ProjectFiles.astro';

Addressable RGB LED strip with effects and OTA updates.

## Hardware
- ESP8266/ESP32 microcontroller
- WS2812B (Neopixel) addressable LED strip
- WiFi/MQTT connectivity

## Files & downloads

<ProjectFiles slug="neopixel-strip" />
